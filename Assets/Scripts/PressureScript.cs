using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PressureScript : MonoBehaviour
{
    /*private bool activated = false;
    public int mass = 3;

    public Sprite sprite1;
    public Sprite sprite2;

    private SpriteRenderer spriteRenderer;

    void OnCollisionEnter2D(Collider2D otherCollider)
    {
        if (otherCollider.gameObject.GetComponent<Rigidbody2D>().mass >= mass) {
            this.activated = !this.activated;

            if (spriteRenderer == null)
            {
                spriteRenderer = GetComponent<SpriteRenderer>();
            }

            if (spriteRenderer.sprite == sprite1)
            {
                spriteRenderer.sprite = sprite2;
            }
        }
    }

    void OnCollisionExit2D(Collider2D otherCollider)
    {
          
            this.activated = !this.activated;

            if (spriteRenderer == null)
            {
                spriteRenderer = GetComponent<SpriteRenderer>();
            }

            if (spriteRenderer.sprite == sprite2)
            {
                spriteRenderer.sprite = sprite1;
            }

        
    }*/
}
